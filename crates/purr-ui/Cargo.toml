[package]
name = "purr-ui"
version = "0.1.0"
authors = ["Gaëtan S. <>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "purr_ui"
path = "src/lib.rs"

[[bin]]
name = "purr-ui"
path = "src/main.rs"


[dependencies]
# Desktop-only dependencies
purr-core = { path = "../purr-core", optional = true }
purr-common = { path = "../purr-common" }
tokio = { version = "1.47.1", default-features = false }
tracing-subscriber = { version = "0.3" }

# Shared dependencies (work on both WASM and native)
dioxus = { version = "0.7.0-rc.0", features = ["router"] }
futures = { version = "0.3.31", default-features = false, features = [
  "async-await",
  "alloc",
  "std",
] }
async-trait = "0.1"
serde = { version = "1.0.225", features = ["derive"] }
serde_json = { version = "1.0", optional = true }
bytes = "1.10.1"
uuid = { version = "1.5.0", features = ["v4", "js", "v3"] }
tracing = "0.1.41"
miette = { version = "7.6.0", features = ["fancy"] }
thiserror = "2.0.9"
chrono = { version = "0.4", features = ["serde"], optional = true }

# Web-only dependencies (only for WASM builds)
gloo-timers = { version = "0.3.0", features = ["futures"], optional = true }
gloo-file = { version = "0.3.0", optional = true }
web-sys = { version = "0.3", features = [
  "Window",
  "Request",
  "RequestInit",
  "Response",
  "FormData",
  "Blob",
  "BlobPropertyBag",
  "console",
], optional = true }
js-sys = { version = "0.3", optional = true }
wasm-bindgen = { version = "0.2", features = [
  "serde-serialize",
], optional = true }
wasm-bindgen-futures = { version = "0.4", optional = true }
serde-wasm-bindgen = { version = "0.6", optional = true }
getrandom = { version = "0.2", features = ["js"], optional = true }

# Desktop-only dependencies
rfd = { version = "0.15.4", features = [
  "tokio",
  "xdg-portal",
], default-features = false, optional = true }
cached = { version = "0.56.0", optional = true }
const-str = { version = "0.7.0", features = ["all"] }
purr-wasm = { path = "../purr-wasm", optional = true }
scc = { version = "3.1.2", features = ["serde"] }
serde_with = "3.14.1"
strum = { version = "0.27.2", features = ["derive"] }
ambassador = "0.4.2"

[features]
default = ["desktop"]

# Web feature includes only WASM-compatible dependencies
web = [
  "dioxus/web",
  "gloo-timers",
  "gloo-file",
  "web-sys",
  "js-sys",
  "wasm-bindgen",
  "wasm-bindgen-futures",
  "serde-wasm-bindgen",
  "getrandom",
  "serde_json",
  "purr-wasm",
]

# Desktop feature includes desktop-specific functionality and transcription
desktop = [
  "dioxus/desktop",
  "tokio/full",
  "purr-core",
  "rfd",
  "cached",
  "serde_json",
  "chrono",
]

# Mobile feature
mobile = ["dioxus/mobile"]

vulkan = ["purr-core/vulkan"]
cuda = ["purr-core/cuda"]
metal = ["purr-core/metal"]
coreml = ["purr-core/coreml"]
gloo-timers = ["dep:gloo-timers"]

[dev-dependencies]
rstest = "0.26"
pretty_assertions = "1.4.1"
tempfile = "3.21"
wasm-bindgen-test = "0.3"
